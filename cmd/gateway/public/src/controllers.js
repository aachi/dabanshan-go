/*global define */

'use strict';

define(function() {
    var controllers = {};

    controllers.HomeCtrl = function($scope, $rootScope) {

    }
    controllers.HomeCtrl.$inject = ['$scope', '$rootScope'];
    
    return controllers;
});